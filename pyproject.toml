[project]
name = "aegisroute"
version = "0.1.0"
description = "Minimal AI gateway skeleton"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115,<1.0",
    "pydantic>=2.0,<3.0",
    "uvicorn>=0.30,<1.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0,<10.0",
]
